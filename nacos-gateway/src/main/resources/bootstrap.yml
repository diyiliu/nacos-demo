server:
  port: 8090
spring:
  application:
    name: nacos-gateway-demo
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
    gateway:
      discovery:
        locator:
          enabled: true
          lowerCaseServiceId: true